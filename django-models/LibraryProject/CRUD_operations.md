# Django ORM CRUD Operations for Book Model

This document details the Create, Retrieve, Update, and Delete operations performed on the `bookshelf.Book` model via the Django shell.

## Pre-requisite: Enter Django Shell

```bash
python manage.py shell